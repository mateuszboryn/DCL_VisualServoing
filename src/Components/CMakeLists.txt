# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(Mrrocpp)

ADD_COMPONENT(VisualServoIB)

ADD_COMPONENT(VisualServoPB)

ADD_COMPONENT(NewestImage)

ADD_COMPONENT(EventTimestamp)
